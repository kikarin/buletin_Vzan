function BackButton({ onClick }) {
    return (
      <button className="text-blue-600 hover:underline mb-4" onClick={onClick}>
        ← Kembali
      </button>
    );
  }
  
  export default BackButton;
  